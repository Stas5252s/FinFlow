<div align="center">

# Finflow 💰

**Личный трекер доходов и расходов — один файл, ноль зависимостей**

[🚀 Открыть](#быстрый-старт) · [✨ Возможности](#возможности) · [🛠 Архитектура](#архитектура) · [📦 Деплой](#деплой)

---

![theme](https://img.shields.io/badge/тема-тёмная%20%2F%20светлая-5b8cff?style=flat-square)
![i18n](https://img.shields.io/badge/язык-EN%20%7C%20UK%20%7C%20RU-00e5a0?style=flat-square)
![size](https://img.shields.io/badge/размер-78.5%20KB-ff4e6a?style=flat-square&logo=html5&logoColor=white)
![deps](https://img.shields.io/badge/установка-не%20нужна-brightgreen?style=flat-square)

</div>

---

## Что это

Finflow — однофайловый HTML-трекер личных финансов. Открывается прямо в браузере без установки, сервера и регистрации. Данные хранятся локально в `localStorage`, поэтому не теряются при перезагрузке.

---

## Возможности

### 🏠 Первый экран / онбординг
- **Hero-секция** с заголовком и подзаголовком появляется при первом открытии (нет транзакций) и скрывается сразу после добавления первой записи
- **5 демо-транзакций** показываются полупрозрачно со штрихованной рамкой — сразу виден функционал приложения, без ввода данных
- **Empty state** с иконкой, описанием и кнопкой «Добавить первую транзакцию»

### 💳 Транзакции
- Добавление **доходов** и **расходов** через модальную форму
- Поля: сумма + валюта, категория, дата, комментарий
- Подсказки прямо в форме: формат числа (`1500.00`), формат даты (`дд.мм.гггг`)
- Анимация появления (`translateY + scale`) и удаления (slide-out)
- Кнопка удаления скрыта по умолчанию и появляется при наведении на карточку

### 🗂 Категории
| ID | Название (RU) | Тип | Иконка |
|---|---|---|---|
| `salary` | Зарплата | доход | briefcase |
| `gifts` | Подарки | доход | gift |
| `income_other` | Прочее (доходы) | доход | coins |
| `food` | Еда | расход | burger |
| `transport` | Транспорт | расход | bus |
| `entertainment` | Развлечения | расход | gamepad |
| `housing` | Жильё | расход | home |
| `expense_other` | Прочее (расходы) | расход | receipt |

- Встроенные категории — защищены от удаления, переводятся при смене языка
- Пользовательские категории — создаются из формы транзакции или из менеджера категорий
- **16 SVG-иконок** на выбор: `briefcase gift coins burger bus gamepad home receipt tag star heart shoppingbag car music coffee plane`

### 📊 Графики
- **Кольцевая диаграмма** — расходы по категориям
- **Линейная диаграмма** — динамика баланса во времени
- Для каждого графика — независимый выбор валюты отображения (с конвертацией)
- Реактивное обновление при любом изменении данных или фильтров

### 💱 Мультивалютность
- **8 валют** на выбор для каждой транзакции: UAH, USD, EUR, GBP, PLN, CHF, CZK, JPY
- **Live-тикер** в верхней полосе: курс каждой валюты к ₴, стрелки ▲▼ при изменении, время обновления
- Автообновление курсов каждые **60 секунд** + при возврате на вкладку
- Fallback-цепочка: API → кэш в localStorage → встроенные значения
- Индикатор статуса: 🟢 LIVE / 🟡 Загрузка / 🔴 Офлайн / Кэш
- Ручное обновление кнопкой (с анимацией вращения)
- Все карточки баланса автоматически конвертируют суммы в UAH

### 🔍 Фильтры
- **Период**: день / неделя / месяц / все
- **Тип**: доходы / расходы / все
- **Категории**: мультиселект, несколько одновременно
- Чипы категорий подсвечиваются: зелёный для доходов, красный для расходов
- Все три фильтра работают одновременно, без перезагрузки

### 🎨 UI/UX
- Кнопки «+ Доход» и «− Расход» — **solid-стиль** с тенью и hover-эффектами (`box-shadow`, `translateY`)
- Карточки баланса — иконки, тени, плавный подъём при наведении
- Баланс — крупный шрифт `clamp(2rem, 4vw, 2.8rem)`, зелёный или красный в зависимости от знака
- Модальные окна — плавная анимация открытия `cubic-bezier(.34, 1.4, .64, 1)` (пружина)
- Шум-текстура на фоне через SVG-фильтр (только в тёмной теме)
- Кастомный скроллбар

### 🌗 Тёмная / светлая тема
- Переключатель в шапке
- Все цвета через CSS-переменные — мгновенный переход без мигания
- Графики перекрашиваются при смене темы
- Выбор сохраняется в `localStorage`

### 🌐 Три языка

| Ключ | 🇬🇧 EN | 🇺🇦 UK | 🇷🇺 RU |
|---|---|---|---|
| `addIncome` | + Income | + Дохід | + Доход |
| `addExpense` | − Expense | − Витрата | − Расход |
| `noTxTitle` | No transactions yet | Транзакцій ще немає | Транзакций пока нет |
| `amountHint` | Enter a positive number… | Введіть додатне число… | Введите положительное число… |
| `csvTooltip` | Export current filter as CSV | Експортувати … | Экспорт текущей выборки в CSV |
| `heroBadge` | Personal Finance | Особисті фінанси | Личные финансы |

- Мгновенное переключение без перезагрузки страницы
- Переводятся: весь интерфейс, подписи графиков, тикер курсов, названия встроенных категорий, подсказки в форме, заголовки CSV
- Выбор языка сохраняется в `localStorage`

### 📤 Экспорт CSV
- Выгружает **текущую выборку** (с учётом всех активных фильтров)
- Кнопка CSV имеет `title`-подсказку с описанием действия (переведена на 3 языка)
- Заголовки колонок на выбранном языке
- UTF-8 BOM — корректно открывается в Excel без настройки кодировки

---

## Быстрый старт

### Вариант 1 — просто открыть в браузере
```bash
open index.html        # macOS
start index.html       # Windows
xdg-open index.html    # Linux
```
Никаких команд, серверов и npm не нужно.

### Вариант 2 — локальный сервер (опционально)
```bash
# Python
python3 -m http.server 3000

# Node.js
npx serve .
```
Затем открыть `http://localhost:3000`.

---

## Деплой

### GitHub Pages
```bash
git init
git add index.html README.md
git commit -m "feat: finflow v5"
git branch -M main
git remote add origin https://github.com/your-username/finflow.git
git push -u origin main
```
В настройках репозитория: **Settings → Pages → Source: `main / (root)`**

Приложение будет доступно по адресу:
```
https://your-username.github.io/finflow/
```

### Другие платформы

| Платформа | Способ |
|---|---|
| **Netlify Drop** | Перетащить `index.html` на [app.netlify.com/drop](https://app.netlify.com/drop) |
| **Vercel** | `vercel --prod` |
| **Cloudflare Pages** | Загрузить через dashboard |
| **Любой хостинг** | Загрузить единственный файл |

---

## Архитектура

Весь проект — **один файл `index.html`** (~78 KB), состоящий из трёх частей:

```
index.html
│
├── <style>  ──────────────────────────────── CSS (~400 строк)
│   ├── CSS-переменные тем (dark / light)
│   ├── Hero, Header, Lang switcher, Buttons
│   ├── Rates bar, Balance grid, Filters
│   ├── Transaction list + animations
│   ├── Charts sidebar
│   └── Modal + Form
│
├── <body>  ───────────────────────────────── HTML (~130 строк)
│   ├── header            лого · языки · тема · кнопки · CSV
│   ├── #hero-section     онбординг (скрывается после 1-й транзакции)
│   ├── .rates-bar        live-тикер курсов валют
│   ├── .balance-grid     3 карточки: баланс / доходы / расходы
│   ├── .filter-section   чипы: период · тип · категории
│   ├── .main-grid
│   │   ├── .tx-list      список транзакций (+ demo)
│   │   └── .sidebar      кольцевой + линейный графики
│   ├── #tx-modal         модалка добавления транзакции
│   └── #manage-cats-modal менеджер категорий
│
└── <script>  ─────────────────────────────── JavaScript (~650 строк)
    ├── TRANSLATIONS        словари EN / UK / RU (все ключи)
    ├── ICONS               16 SVG-иконок (Lucide, inline)
    ├── CURRENCIES          8 валют с символами
    ├── FALLBACK_RATES      встроенные курсы (fallback)
    ├── State & Storage     state, KEYS, DEFAULT_CATEGORIES
    ├── getDemoTransactions 5 примеров для первого экрана
    │
    ├── i18n                t(), applyI18n(), setLang(), applyLangUI()
    ├── Theme               toggleTheme(), applyTheme()
    │
    ├── Exchange Rates
    │   ├── loadCachedRates / saveCachedRates   localStorage TTL-кэш
    │   ├── applyRates                          применить + перерисовать
    │   ├── fetchExchangeRates                  open.er-api.com → кэш → fallback
    │   ├── startRatesTimer                     setInterval 60s + visibilitychange
    │   ├── manualRefreshRates                  ручное обновление (кнопка)
    │   ├── setRatesStatus                      dot: live / loading / error
    │   └── renderRatesTicker                   chips с ▲▼ стрелками
    │
    ├── Currency UI         populateCurrencyDropdowns, onChartCurrencyChange
    ├── Icon Selects        populateIconSelects, iconSvg
    │
    ├── Filters             setPeriodFilter, setTypeFilter,
    │                       toggleCatFilter, clearCatFilter,
    │                       getFilteredTransactions
    │
    ├── Render              render, renderStats, renderCatChips,
    │                       renderTransactions, txItemHtml
    │
    ├── Transactions CRUD   saveTransaction, deleteTx
    ├── TX Modal            openTxModal, closeTxModal,
    │                       populateCategorySelect, addCatFromForm
    │
    ├── Categories CRUD     openManageCatsModal, renderCatManager,
    │                       addCatFromManager, deleteCat
    │
    ├── Charts              renderCharts, renderPieChart, renderLineChart
    ├── CSV Export          exportCSV
    └── Boot                init()
```

### localStorage — схема данных

| Ключ | Тип | Содержимое |
|---|---|---|
| `ff5_txs` | JSON array | Массив транзакций |
| `ff5_cats` | JSON array | Массив категорий (включая пользовательские) |
| `ff5_theme` | string | `"dark"` или `"light"` |
| `ff5_lang` | string | `"en"`, `"uk"` или `"ru"` |
| `ff5_rates` | JSON object | `{ rates: {...}, ts: timestamp }` |

#### Схема транзакции
```json
{
  "id": "lrx4k2ab",
  "type": "expense",
  "amount": 480,
  "currency": "UAH",
  "categoryId": "food",
  "date": "2026-02-18",
  "comment": "Обед"
}
```

#### Схема категории
```json
{
  "id": "my_cat_xyz",
  "name": "Кофе",
  "iconKey": "coffee",
  "type": "expense",
  "custom": true
}
```
Встроенные категории имеют `"custom": false` и `"i18nKey"` вместо фиксированного `name`.

---

## Внешние зависимости

| Ресурс | Версия | Подключение | Требуется для |
|---|---|---|---|
| [Chart.js](https://www.chartjs.org/) | 4.4.1 | CDN (cdnjs) | Графики |
| [Syne](https://fonts.google.com/specimen/Syne) | — | Google Fonts | Заголовки, числа |
| [DM Sans](https://fonts.google.com/specimen/DM+Sans) | — | Google Fonts | Основной текст |
| [open.er-api.com](https://www.exchangerate-api.com/) | v6 | fetch | Курсы валют (live) |

> Все SVG-иконки встроены инлайн — дополнительных запросов нет.

**Для полного офлайн-режима:**
1. Скачать Chart.js: `https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js`
2. Заменить `src` на локальный путь
3. Загрузить шрифты локально через `@font-face`
4. Курсы будут использовать встроенный fallback

---

## Браузерная поддержка

| Браузер | Версия |
|---|---|
| Chrome / Edge | 90+ |
| Firefox | 88+ |
| Safari | 14+ |
| Mobile Chrome | ✅ |
| Mobile Safari | ✅ |

Используемые возможности браузера: `localStorage`, `fetch`, CSS custom properties, CSS Grid, `backdrop-filter`, `clamp()`.

---

## Ограничения

- **Только локальные данные** — `localStorage` не синхронизируется между устройствами и браузерами
- **Объём localStorage** — обычно 5–10 МБ; достаточно для десятков тысяч транзакций
- **Курсы валют** зависят от доступности `open.er-api.com`; при недоступности работает на кэше или встроенных значениях
- **Нет редактирования** транзакций — только удаление и добавление заново

---

## Лицензия

[MIT](https://opensource.org/licenses/MIT) — используйте свободно, в том числе в коммерческих проектах.
